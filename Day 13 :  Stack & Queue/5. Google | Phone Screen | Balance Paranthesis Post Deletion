/*
You are given a string containing only parentheses ('(' and ')') and digits (0-9). At the index of a digit, you must delete a number of parentheses to the left of the index equal to the digit's value. Return true if it is possible to balance the parenthesis of the input.

Example 1 Input: ((2)) Output: False Reason: Only possible string is unbalanced: "))"

Example 2 Input: ((((2)) Output: True Reason: Only possible string is balanced: "(())"

Example 3 Input: (()1(1)) Output: True Reason: Multiple possible strings and one is balanced: ")())", "(())", "()))".

Background: 

1.Digits should not be included in the result string, although that doesn't matter for calculating the boolean result.

2.If a digit doesn't have enough characters to the left to delete, then return false.

*/

#include <iostream>
#include <string>

bool canBalanceParentheses(const std::string& s) {
    int open_count = 0;

    for (char c : s) {
        if (c == '(') {
            open_count++;
        } else if (c == ')') {
            open_count--;
            if (open_count < 0) {
                return false; // More ')' than '('
            }
        } else if (isdigit(c)) {
            int digit = c - '0'; // Convert char to int
            if (open_count < digit) {
                return false; // Not enough '(' to delete
            }
            open_count -= digit; // Simulate the deletion of '('
        }
    }
    
    return open_count == 0; // Finally check for balance
}

int main() {
    std::string input1 = "((2))"; // Output: false
    std::string input2 = "((((2))"; // Output: true
    std::string input3 = "(()1(1))"; // Output: true
    
    std::cout << std::boolalpha; // Print true/false as bool
    std::cout << "Test 1: " << canBalanceParentheses(input1) << std::endl;
    std::cout << "Test 2: " << canBalanceParentheses(input2) << std::endl;
    std::cout << "Test 3: " << canBalanceParentheses(input3) << std::endl;

    return 0;
}


/*
To determine whether we can balance the parentheses in the given string based on the rules defined, we can create a C++ function that processes the string. The goal is to ensure that every digit in the string specifies the number of left parentheses we need to delete, and after processing all digits, the remaining parentheses must be balanced.

Here’s a step-by-step explanation of our approach:

### Explanation and Steps

1. **Initialization**:
   - We will create a counter for open parentheses (`open_count`). This will keep track of the number of unmatched `(` parentheses.
   - We will also use a variable to track the index in the string as we process it.

2. **Iterate through the string**:
   - For each character in the string:
     - If it's an opening parenthesis `'('`, increment `open_count`.
     - If it's a closing parenthesis `')'`, decrement `open_count`. If `open_count` becomes negative, it means we have unmatched closing parentheses, and we can immediately return `false`.
     - If it's a digit, convert it to an integer (`digit`), and check if we can delete that many opening parentheses. If `open_count` is less than `digit`, return `false`.
     - If there are enough opening parentheses, decrement `open_count` by `digit` (effectively simulating the deletion).

3. **Final Check**:
   - After processing the entire string, if `open_count` is zero, it means all parentheses can be balanced; otherwise, return `false`.

### C++ Code Implementation

Here is the implementation in C++:

```cpp

```

### Dry Run Analogy

Let's take the input `(()1(1))` and walk through it using our algorithm:

1. Start with `open_count = 0`.
2. Process `(`: `open_count` becomes 1.
3. Process `(`: `open_count` becomes 2.
4. Process `)`: `open_count` becomes 1.
5. Process `1`: 
    - `digit = 1`, check `open_count (1) >= digit (1)` → true.
    - Decrement `open_count` by `digit` → now `open_count = 0`.
6. Process `(`: `open_count` becomes 1.
7. Process `1`: 
    - `digit = 1`, check `open_count (1) >= digit (1)` → true.
    - Decrement `open_count` by `digit` → now `open_count = 0`.
8. Process `)`: `open_count` becomes -1 (it's negative, indicating an unmatched `)`).
9. End of the string, check `open_count == 0` → false.

In this case, we would return `false` for input `(()1(1))`, indicating that it’s impossible to balance the parentheses.

### Conclusion

The provided code reliably assesses whether the parentheses in the input string can be balanced according to the rules.
The checks for unmatched parentheses and sufficient deletion make the function robust and accurate for the problem statement.*/
